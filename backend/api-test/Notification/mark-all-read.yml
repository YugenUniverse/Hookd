info:
    name: mark-all-read
    type: http
    seq: 3

http:
    method: PATCH
    url: http://localhost:3000/notifications/read-all
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
