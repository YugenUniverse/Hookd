info:
    name: unread-count
    type: http
    seq: 4

http:
    method: GET
    url: http://localhost:3000/notifications/unread-count
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
