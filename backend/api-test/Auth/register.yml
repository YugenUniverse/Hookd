info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
          "email": "celli@celli.it",
          "username": "celli",
          "password": "celli",
          "userType": "Facility",
          "name": "Celli's"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
