info:
    name: login
    type: http
    seq: 1

http:
    method: POST
    url: http://localhost:3000/auth/login
    body:
        type: json
        data: |-
            {
              "email":"celli@celli.it",
              "password":"celli"
            }
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
