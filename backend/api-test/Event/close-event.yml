info:
    name: close-event
    type: http
    seq: 7

http:
    method: POST
    url: http://localhost:3000/events/:id/close
    params:
        - name: id
          value: event_id_here
          type: path
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
