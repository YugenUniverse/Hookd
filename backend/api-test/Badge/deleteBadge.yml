info:
  name: deleteBadge
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/badges/:id
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
