info:
    name: update-session
    type: http
    seq: 8

http:
    method: PUT
    url: http://localhost:3000/sessions/<SESSION_ID>
    body:
        type: json
        data: |-
            {
              "time": 95,
              "date": "2026-05-20"
            }
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
