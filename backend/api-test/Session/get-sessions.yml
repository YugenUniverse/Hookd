info:
    name: get-sessions
    type: http
    seq: 6

http:
    method: GET
    url: http://localhost:3000/session
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
