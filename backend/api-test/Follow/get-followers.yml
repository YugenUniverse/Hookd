info:
    name: get-followers
    type: http
    seq: 5

http:
    method: GET
    url: http://localhost:3000/follows/:userId/followers
    params:
        - name: userId
          value: 6a021092a362ceb8f7bd15fd
          type: path
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
