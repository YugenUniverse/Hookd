info:
    name: get-following
    type: http
    seq: 3

http:
    method: GET
    url: http://localhost:3000/follows/me
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
