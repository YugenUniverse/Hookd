info:
    name: follow-user
    type: http
    seq: 1

http:
    method: POST
    url: http://localhost:3000/follows/:targetId
    params:
        - name: targetId
          value: 6a021092a362ceb8f7bd15fd
          type: path
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
