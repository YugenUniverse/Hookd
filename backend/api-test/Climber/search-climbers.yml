info:
    name: search-climbers
    type: http
    seq: 3

http:
    method: GET
    url: http://localhost:3000/climbers/search
    params:
        - name: q
          value: alex
          type: query
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
