info:
    name: admin-list-tickets
    type: http
    seq: 4

http:
    method: GET
    url: http://localhost:3000/admin/support/tickets
    params:
        - name: status
          value: OPEN
          enabled: false
        - name: category
          value: BUG
          enabled: false
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
