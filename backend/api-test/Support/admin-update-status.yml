info:
    name: admin-update-status
    type: http
    seq: 6

http:
    method: PATCH
    url: http://localhost:3000/admin/support/tickets/6a021092a362ceb8f7bd15fd/status
    body:
        type: json
        data: |-
            {
              "status": "CLOSED"
            }
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
