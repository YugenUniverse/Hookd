info:
    name: create-ticket
    type: http
    seq: 1

http:
    method: POST
    url: http://localhost:3000/support
    body:
        type: json
        data: |-
            {
              "subject": "Cannot login to my account",
              "body": "I have been unable to login for the past 2 days. I get an error message.",
              "category": "ACCOUNT"
            }
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
