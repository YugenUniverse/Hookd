info:
    name: get-pending-invites
    type: http
    seq: 8

http:
    method: GET
    url: http://localhost:3000/groups/invites/pending
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
