info:
    name: decline-invite
    type: http
    seq: 10

http:
    method: PATCH
    url: http://localhost:3000/groups/invites/:inviteId/decline
    params:
        - name: inviteId
          value: 6a021092a362ceb8f7bd15fd
          type: path
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
