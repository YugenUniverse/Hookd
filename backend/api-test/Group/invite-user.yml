info:
    name: invite-user
    type: http
    seq: 6

http:
    method: POST
    url: http://localhost:3000/groups/:id/invites
    params:
        - name: id
          value: 6a021092a362ceb8f7bd15fd
          type: path
    body:
        type: json
        data: |-
            {
                "username": "alex_climbs"
            }
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
