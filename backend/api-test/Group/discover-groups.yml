info:
    name: discover-groups
    type: http
    seq: 15

http:
    method: GET
    url: http://localhost:3000/groups/discover
    params:
        - name: search
          value: boulder
          type: query
          enabled: false
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
