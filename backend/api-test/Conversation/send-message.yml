info:
    name: send-message
    type: http
    seq: 5

http:
    method: POST
    url: http://localhost:3000/conversations/:id/messages
    params:
        - name: id
          value: 6a021092a362ceb8f7bd15fd
          type: path
    body:
        type: json
        data: |-
            {
                "content": "Hey, want to plan a climb this weekend?"
            }
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
