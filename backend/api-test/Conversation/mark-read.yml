info:
    name: mark-read
    type: http
    seq: 6

http:
    method: PATCH
    url: http://localhost:3000/conversations/:id/read
    params:
        - name: id
          value: 6a021092a362ceb8f7bd15fd
          type: path
    auth:
        type: bearer
        token:

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
