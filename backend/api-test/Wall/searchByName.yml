info:
  name: searchByName
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/walls/search?q=Celli
  params:
    - name: q
      value: Celli
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
